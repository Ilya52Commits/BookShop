<Page x:Class="BookShopCore.Views.RegistrationView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:viewModels="clr-namespace:BookShopCore.ViewModels"
      mc:Ignorable="d"
      d:DesignWidth="800"
      Title="RegistrationView">

    <Page.Resources>
        <ResourceDictionary Source="../StyleDictionary.xaml"/>
    </Page.Resources>

    <Page.DataContext >
        <viewModels:RegistrationViewModel/>
    </Page.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Style="{DynamicResource BorderContainerStyles}">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" BlurRadius="20" />
            </Border.Effect>
            <StackPanel>
                <DockPanel LastChildFill="True" Grid.Row="1" Margin="5">
                    <StackPanel Width="300" Margin="30">
                        <Label Content="РЕГИСТРАЦИЯ" FontSize="20" HorizontalContentAlignment="Center" />
                        <StackPanel Margin="0, 10, 0, 0">
                            <TextBlock Text="Логин" FontSize="20" />
                            <Border Style="{DynamicResource TextBoxBorderStyles}">
                                <TextBox 
                                    Text="{Binding Login}" 
                                    Style="{DynamicResource TextBoxStyles}" 
                                />
                            </Border>
                        </StackPanel>
                        <StackPanel Margin="0, 10, 0, 0">
                            <TextBlock Text="Email" FontSize="20" />
                            <Border Style="{DynamicResource TextBoxBorderStyles}">
                                <TextBox 
                                    Text="{Binding Email}" 
                                    Style="{DynamicResource TextBoxStyles}"
                                />
                            </Border>
                        </StackPanel>
                        <StackPanel Margin="0, 10, 0, 0">
                            <TextBlock Text="Пароль" FontSize="20" />
                            <Border Style="{DynamicResource TextBoxBorderStyles}">
                                <TextBox 
                                    Text="{Binding Password}"     
                                    Style="{DynamicResource TextBoxStyles}"
                                />
                            </Border>
                        </StackPanel>
                        <StackPanel Margin="0, 10, 0, 0">
                            <TextBlock Text="Повторите пароль" FontSize="20" />
                            <Border Style="{DynamicResource TextBoxBorderStyles}">
                                <TextBox 
                                    Text="{Binding ConfigPassword}"
                                    Style="{DynamicResource TextBoxStyles}"
                                />
                            </Border>
                        </StackPanel>
                        <Border Style="{DynamicResource ButtonBorderStyles}">
                            <Button 
                                Command="{Binding RegistrationClientCommand}" 
                                Style="{DynamicResource ButtonStyles}"
                                Content="Зарегистрироваться"
                            />
                        </Border>
                    </StackPanel>
                </DockPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
